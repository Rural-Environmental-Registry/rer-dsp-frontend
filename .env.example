# Base do SPA (deve bater com o nginx / proxy)
VITE_BASE_URL=/dsp/

# URL da API — tem prioridade sobre public/config/env.json
# Dev local (backend no host):
VITE_DSP_API_URL=http://localhost:8080/dsp-backend
# Docker / proxy (URL relativa ao host):
# VITE_DSP_API_URL=/dsp-backend
