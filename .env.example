# SPA base path (must match nginx / proxy)
VITE_BASE_URL=/dsp/

# API URL — takes priority over public/config/env.json
# Local dev (backend on host):
VITE_DSP_API_URL=http://localhost:8080/dsp-backend
# Docker / proxy (URL relative to host):
# VITE_DSP_API_URL=/dsp-backend
